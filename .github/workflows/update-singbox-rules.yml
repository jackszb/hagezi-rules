name: Update sing-box rules

on:
  schedule:
    - cron: "0 0 * * *"  # 每天 UTC 00:00 自动更新
  workflow_dispatch:    # 支持手动触发

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install sing-box
        run: |
          curl -fsSL https://sing-box.app/install.sh | sh
          sing-box version

      - name: Prepare rules directory
        run: |
          mkdir -p rules

      - name: Generate sing-box rule source files
        run: |
          set -e

          declare -A RULES=(
            ["light"]="https://raw.githubusercontent.com/hagezi/dns-blocklists/main/domains/light.txt"
            ["multi"]="https://raw.githubusercontent.com/hagezi/dns-blocklists/main/domains/multi.txt"
            ["pro"]="https://raw.githubusercontent.com/hagezi/dns-blocklists/main/domains/pro.txt"
            ["pro.plus"]="https://raw.githubusercontent.com/hagezi/dns-blocklists/main/domains/pro.plus.txt"
            ["ultimate"]="https://raw.githubusercontent.com/hagezi/dns-blocklists/main/domains/ultimate.txt"
            ["tif"]="https://raw.githubusercontent.com/hagezi/dns-blocklists/main/domains/tif.txt"
          )

          for name in "${!RULES[@]}"; do
            tmp="$(mktemp)"

            # 下载并处理域名：去除表头、空行，去重、排序
            curl -fsSL "${RULES[$name]}" \
              | sed 's/\r//' \
              | sed '/^$/d' \
              | sed '/^#/d' \
              | sort -u > "$tmp"

            # 生成 sing-box JSON，加入 version:3
            jq -Rn '
              [inputs] |
              {
                version: 3,
                rules: [
                  {
                    domain_suffix: .
                  }
                ]
              }
            ' "$tmp" > "rules/${name}.json"

            rm -f "$tmp"
          done

      - name: Compile rule-set to .srs
        run: |
          for json in rules/*.json; do
            sing-box rule-set compile "$json"
          done

      - name: Commit & Push rules
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "GitHub Actions"

          git add rules/
          git commit -m "Auto update sing-box rules" || echo "No changes"
          git push
